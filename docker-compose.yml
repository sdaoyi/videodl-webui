version: "3.9"

services:
  videodl:
    build: .
    container_name: videodl-webui
    restart: unless-stopped
    ports:
      - "9999:9999"
    volumes:
      - ./videodl_outputs:/app/videodl_outputs
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
